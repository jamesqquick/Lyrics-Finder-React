import React, { Component } from 'react';

export default class Navbar extends Component {
    render() {
        return (
            <nav className="nav">
                <h2 className="nav-title">Lyric Finder</h2>
            </nav>
        );
    }
}
